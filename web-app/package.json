{
	"name": "demos",
	"version": "0.1.0",
	"private": true,
	"engines": 
		{"node": ">=7.7",
		 "npm": ">=4.3"
		},
	"comment_only": "// npm-check-updates -u",
	"scripts": {
		"start": "node index"
	},
	"dependencies": { 
		"express": ">=4.1"
		, "cors": ">=2.8"
		, "compression": ">=1.6"
		, "body-parser": ">=1.1"
		, "is_js": ">=0.9"
		, "useragent": ">=2.1"
		, "now-logs": ">=0.0.7"
		, "bugsnag": ">=1.9"
		, "topseed-util": "=1.0.3"
		, "riot": ">=3.2"
		, "cheerio": ">=0.22"
		, "node-fetch": ">=1.6"

		, "jsonwebtoken": ">= 7.2"

	}
}
